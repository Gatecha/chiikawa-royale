# Chiikawa Royale Project Rules

## Git Workflow
- Always stage (`git add`), commit (`git commit -m "..."`), and push (`git push`) your code modifications automatically to the remote repository upon completing a task, without asking for manual confirmation first.
